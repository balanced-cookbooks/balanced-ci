set +x # Redact credentials from log
echo Setting AWS credentials
export AWS_ACCESS_KEY_ID="<%= @citadel.access_key_id %>"
export AWS_SECRET_ACCESS_KEY="<%= @citadel.secret_access_key %>"
export AWS_TOKEN="<%= @citadel.token %>"
set -x
